# CLAUDE.md

此文件为此项目的CLAUDE CODE AI协作提供指导。

## 编写语言

尽量全部使用Python编写，如必须使用其他语言必须告知。

## 项目结构

根目录：存放源代码和相关存储文件
`lang/`：存放语言文件
`.venv`：此项目专用虚拟环境

## 重要文件及作用

`disk_link.py`：程序主要模块
`errors.py`：错误处理模块，目前还未完全从主要模块分离
`output.py`：输出模块，使用colorama库
`快捷启动.py`：在硬盘中运行时用于链接到主程序的程序

## 环境要求

- 当需要下载第三方库时必须提醒。
- 第三方库下载在虚拟环境中。
